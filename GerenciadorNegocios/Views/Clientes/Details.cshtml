@model GerenciadorNegocios.Models.Cliente

@{
    ViewBag.Title = "Detailhes do cliente";
}

<h2>@ViewBag.Title</h2>

<div>
    <h4>Cliente</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt class="success">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nome)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CPFCNPJ)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CPFCNPJ)
        </dd>

    </dl>
</div>
<div class="form-group">
    <div class="col-md-offset-2 col-md-10">
        <input type="button"
               value="Editar"
               class=" btn btn-success"
               onclick="location.href='@Url.Action("Edit", "Clientes", new { id = Model.Id })'" />
        <input type="button"
               value="Voltar"
               class=" btn btn-primary"
               onclick="location.href='@Url.Action("Index", "Clientes")'" />
    </div>
</div>

